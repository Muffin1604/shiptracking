from django.urls import path

from ports import views

urlpatterns = [
    # path(
    #     "ports/<str:port_unlocode>/calls/",
    #     views.PortCallsByPortView.as_view(),
    #     name="port-calls-by-port",
    # ),
]
